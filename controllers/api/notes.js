const Note = require("../../models/note");

// module.exports = {
//   create,
// };

// async function create(req, res) {
//   req.body.user = req.user;
//   const newNote = new Note();
//   try {
//     await Notes.create(req.body);
//     res.redirect("/notes");
//   } catch (error) {
//     console.log(err);
//     res.render("/notes/:id", { errorMsg: err.message });
//   }
// }
